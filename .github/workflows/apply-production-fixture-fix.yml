name: Apply production fixture fix

on:
  push:
    branches:
      - hotfix/typecheck-1.0.1
    paths:
      - .github/production-fixture-fix.patch

permissions:
  contents: write

jobs:
  apply-fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: hotfix/typecheck-1.0.1
          fetch-depth: 0
      - name: Apply fixture update
        shell: bash
        run: |
          set -euo pipefail
          git apply --check .github/production-fixture-fix.patch
          git apply .github/production-fixture-fix.patch
          rm .github/production-fixture-fix.patch
          rm .github/workflows/apply-production-fixture-fix.yml
          git config user.name "BrisaBase CI"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "test: align production fixture with AI allowlist"
          git push origin HEAD:hotfix/typecheck-1.0.1
